generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

// TODO: Delete this and write your own models
model DummyModel {
    id BigInt @id
}
